{
  "scripts": {
    "build": "spago build -u '--censor-lib --strict'",
    "test": "spago -x test.dhall test -u '--censor-lib'"
  },
  "devDependencies": {
    "purescript": "^0.14.0",
    "purescript-psa": "^0.8.2",
    "spago": "^0.19.1"
  }
}
